import jsonServerProvider from "ra-data-json-server";

/**
 * Returns ra-data-json-server directly for now. In the future, this
 * function can be extended to customize the data provider to better
 * fit Spring Boot APIs.
 */
export default jsonServerProvider;
